apiVersion: v1
kind: Service
metadata:
  name: wso2-identity-server
  labels:
    app.kubernetes.io/name: identity-server
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 9443
      targetPort: 9443
  selector:
    app.kubernetes.io/name: identity-server
